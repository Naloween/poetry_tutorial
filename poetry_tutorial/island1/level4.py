
"""level4
"""

def number_length(number_a: int) -> int:
    """length of a number

    Args:
        number_a (int): the number to get the length

    Returns:
        int: the length of the number a
    """
    return len(str(number_a))
