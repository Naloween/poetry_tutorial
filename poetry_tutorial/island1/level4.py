
def number_length(a: int) -> int:
    """length of a number

    Args:
        a (int): the number to get the length

    Returns:
        int: the length of the number a
    """
    return len(str(a))
